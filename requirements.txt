# Core
langchain==0.3.25
langchain-community==0.3.25
langchain-openai==0.3.16
openai==1.82.0

# PDF parsing
pymupdf==1.25.5
pdfplumber==0.11.6

# Vector store
faiss-cpu==1.11.0

# Embeddings (local fallback)
sentence-transformers==4.1.0

# ── Week 2 additions ──────────────────────────────────────────────────────────

# BM25 keyword search (hybrid retrieval)
rank-bm25==0.2.2

# Cross-encoder reranker (runs locally, no API cost)
# Model: cross-encoder/ms-marco-MiniLM-L-6-v2  (~80MB download on first run)
sentence-transformers==4.1.0   # already listed above, covers cross-encoder too

# ── Week 3 additions ──────────────────────────────────────────────────────────

# LangGraph — agent loop / state machine
langgraph==0.3.21

# Neo4j Python driver — knowledge graph storage
neo4j==5.28.1

# spaCy — fast local NER for entity extraction
# After pip install, also run: python -m spacy download en_core_web_sm
spacy==3.8.4

# UI
streamlit==1.45.1
streamlit-agraph==0.0.45        # knowledge graph visualisation in Streamlit

# Utilities
python-dotenv==1.1.0
tqdm==4.67.1